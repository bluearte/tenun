[project]
name = "tenun"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "efrizal", email = "efrizal_hardiman@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "openimageio>=3.1.16.0",
    "pydantic>=2.13.5",
]

[project.scripts]
tenun = "tenun:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
